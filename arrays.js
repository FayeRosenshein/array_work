var friends = ["Adriana","Molly","Katy","Clayton"];

var ages = [ 26, 23, 27, 25];

var hasPets = [false, true, true, false];

  // I am calling Adriana, the first
console.log(friends[0]);

// I am calling Molly's age
console.log(ages[1]);

// I am calling Katy's status of having a pet
console.log(hasPets[2]);

// I know that the initial position in an array starts with 0 so every number from the array after will be n-1
// n = number of objects in the array
// element = n-1
// Such as the element 4 will be the fifth object in the array
// And the for the fifth object in the array it will be the element number 4
